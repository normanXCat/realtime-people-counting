# Dépendances du pipeline unique (spec règle 0.5 : versions figées).
#
# Les bornes hautes sont volontairement strictes : une exécution publiée dans le
# mémoire doit pouvoir être reproduite. Les versions effectivement utilisées lors
# du run de référence sont enregistrées dans results/<session>/environment.json.
#
# Installation :
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt      # pytest (suite de tests)

# --- Détection et tracking ------------------------------------------------
ultralytics==8.4.126
torch==2.13.0
torchvision==0.28.0
lap==0.5.13

# --- Traitement vidéo et numérique ---------------------------------------
opencv-python==5.0.0.93
numpy==2.5.2
pillow==12.3.0
# Affectation optimale (matrice N×N) pour la résolution globale des groupes de
# pistes lors d'une correction de swap à 3+ personnes. Importé de façon gardée
# dans identity_manager : sans scipy, le pipeline retombe sur la comparaison par
# paires (jamais d'arrêt au démarrage).
scipy==1.18.1

# --- Configuration et métriques ------------------------------------------
PyYAML==6.0.3

# --- ReID externe (ReID profond, activé par défaut) -----------------------
# L'extracteur profond (`reid.external_reid.enabled: true`) charge OSNet via
# torchreid si le paquet est installé, sinon il retombe proprement sur
# MobileNetV3 Small (torchvision), puis sur le descripteur HSV : aucune de ces
# dégradations n'interrompt le pipeline.
torchreid==0.2.5

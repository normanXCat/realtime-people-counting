# Dépendances du pipeline unique (spec règle 0.5 : versions figées).
#
# Les bornes hautes sont volontairement strictes : une exécution publiée dans le
# mémoire doit pouvoir être reproduite. Les versions effectivement utilisées lors
# du run de référence sont enregistrées dans results/<session>/environment.json.
#
# Installation :
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt      # pytest (suite de tests)

# --- Détection et tracking ------------------------------------------------
ultralytics==8.4.126
torch==2.13.0
torchvision==0.28.0
lap==0.5.13

# --- Traitement vidéo et numérique ---------------------------------------
opencv-python==5.0.0.93
numpy==2.5.2
pillow==12.3.0
# Affectation globale des groupes de 3+ pistes (correction d'inversions) ;
# absent : repli sur la correction par paires.
scipy==1.17.1

# --- Interface web de démonstration (--web, lecture seule) -------------
flask==3.1.2

# --- Configuration et métriques ------------------------------------------
PyYAML==6.0.3

# --- ReID externe (lot 3.a) ----------------------------------------------
# Moteur du descripteur profond (backend onnxruntime) : OSNet-x0.25 exporté en
# ONNX, lu depuis reid.external_reid.model_path (docs/correctif_occlusion.md §16).
onnxruntime==1.30.0
# Le backend `torchreid` (ancien chemin, poids ImageNet) reste optionnel.
# torchreid==0.2.5
